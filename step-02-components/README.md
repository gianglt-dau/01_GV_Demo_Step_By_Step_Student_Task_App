# Step 02 — Tách Component
Mục tiêu: tách `Header` và `Sidebar`, giải thích `export` / `import`.

Điểm giảng:
- Component là function trả về JSX.
- Tên Component bắt đầu bằng chữ hoa.
- `export default` và `import`.
- Cây: `App → Header, Sidebar, Main`.
