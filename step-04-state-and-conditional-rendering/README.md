# Step 04 — State + Conditional Rendering
Mục tiêu:
- `const [page, setPage] = useState('home')`
- `page` là giá trị hiện tại.
- `setPage` là hàm cập nhật State.
- `renderPage()` chọn Page theo State.

Lưu ý: nút Sidebar chưa gọi được `setPage`. Đây là vấn đề cho bước kế tiếp.
