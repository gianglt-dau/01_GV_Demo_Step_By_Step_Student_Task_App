# Step 09 — Xóa Task + ẩn/hiện Sidebar + hoàn chỉnh
Mục tiêu:
- `filter()` để tạo danh sách mới sau khi xóa.
- Conditional Rendering: `{showSidebar && <Sidebar />}`.
- Hệ thống hóa: `Event → State → Render → UI`.
